"""Test package root for the semantic-talent-engine project.

This file exists so pytest can discover tests as a proper package and
so test isolation helpers (fixtures, conftest) resolve consistently
across `tests/unit/` and any future `tests/integration/` directories.
"""
